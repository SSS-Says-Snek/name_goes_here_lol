from src.common import *

import pygame

pygame.init()


def expand_rect_then_shrink(top_right_coord,
                            max_length,
                            width,
                            time_when_pausing,
                            expanding_speed=3,
                            screen=SCREEN):
    pass
    # for now lol, Ima just actually make the game first
